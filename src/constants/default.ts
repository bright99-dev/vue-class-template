/**
 * @description number of pagers. Pagination collapses when the total page count exceeds this value
 */
export const PAGINATION_PAGER_COUNT = 5;

/**
 * @description item count of each page
 */
export const PAGINATION_PAGE_SIZE = 30;

/**
 * @description duration before close. It will not automatically close if set 0
 */
export const NOTIFICATION_DURATION = 3000;

/**
 * @description accepted file types, will not work when thumbnail-mode === true
 */
export const UPLOAD_ACCEPT = 'image/jpeg, image/png, image/jpg, image/webp';

export const STRING_MAX_LENGTH = 191;
